# Home-Server Canary

2026-06-05 — The fleet home-server wake loop went live: the router on `ai.basecradle.com` woke basecradle-ruby AI as the OS user `basecradle-ruby-ai`, proving the full webhook → wake → PR → report loop end-to-end.
